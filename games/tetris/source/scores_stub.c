#include <stdio.h>
#include <time.h>
#include "scores.h"

void savescore(int level) {
    (void)level;
}

void showscores(int level) {
    (void)level;
    printf("High scores are disabled in this port.\n");
}
